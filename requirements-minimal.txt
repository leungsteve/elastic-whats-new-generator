# Minimal requirements for running the Elastic What's New Generator
# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data processing
pyyaml>=6.0.0

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# Testing (optional for runtime)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Elasticsearch (optional - only needed if using real ES storage)
# elasticsearch>=8.11.0

# AI libraries (optional - only needed if using AI features)
# anthropic>=0.7.0
# openai>=1.3.0