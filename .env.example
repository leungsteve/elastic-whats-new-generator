# Elasticsearch Configuration
# For Elastic Serverless (recommended for persistent storage):
ELASTICSEARCH_URL=https://your-project.es.region.provider.cloud.es.io:443
ELASTICSEARCH_API_KEY=your_serverless_api_key_here

# For local Elasticsearch (alternative):
# ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=changeme

# AI Tools - Two-Stage LLM Architecture
# Stage 1: Content Extraction - LLM extracts structured info from scraped docs (cached in ES)
# Stage 2: Presentation Generation - LLM generates presentations from cached extractions
#
# Configure at least ONE of the following API keys (auto-selects: OpenAI > Gemini > Claude)

# OpenAI (Recommended - gpt-4o is balanced quality/cost, gpt-4o-mini is cheapest)
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Custom base URL for OpenAI API (for proxies, Claude Code Max, LiteLLM, etc.)
# Do NOT include /chat/completions - client adds it automatically
# OPENAI_BASE_URL=https://your-proxy-url

# Google Gemini (Alternative - gemini-1.5-flash is fast and cheap)
# GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic Claude (Alternative - claude-3-sonnet is high quality but requires paid credits)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Override default model (defaults: OpenAI=gpt-4o-mini, Gemini=gemini-1.5-flash, Claude=claude-3-sonnet)
# LLM_MODEL=gpt-4o

# Optional AI Tools
ELASTIC_MCP_ENDPOINT=http://localhost:3000
WEB_SEARCH_API_KEY=your_web_search_api_key

# Application Settings
APP_ENV=development
LOG_LEVEL=INFO
DEBUG=true

# Feature Toggle
ENABLE_WEB_SCRAPING=true
ENABLE_AI_GENERATION=true
ENABLE_SAMPLE_DATA_GENERATION=true

# Observability Configuration
OBSERVABILITY_ELASTICSEARCH_URL=https://your-elastic-deployment.elastic-cloud.com
OBSERVABILITY_ELASTICSEARCH_USERNAME=observability_user
OBSERVABILITY_ELASTICSEARCH_PASSWORD=your_password

# OpenTelemetry
OTEL_SERVICE_NAME=elastic-whats-new-generator
OTEL_SERVICE_VERSION=1.0.0
OTEL_RESOURCE_ATTRIBUTES=service.name=elastic-whats-new-generator,service.version=1.0.0

# Sampling
TRACE_SAMPLING_RATIO=1.0  # 100% in dev, 0.1 in prod
LOG_LEVEL=INFO

# Health checks
HEALTH_CHECK_INTERVAL=30
HEALTH_CHECK_TIMEOUT=10